services:
  daemon:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - daemon-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  daemon-data:
