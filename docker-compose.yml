services:
  command-center:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ccc-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  ccc-data:
